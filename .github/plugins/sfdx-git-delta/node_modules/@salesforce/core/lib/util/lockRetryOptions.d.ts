export declare const lockOptions: {
    stale: number;
};
export declare const lockRetryOptions: {
    retries: {
        retries: number;
        maxTimeout: number;
        factor: number;
    };
    fs: import("../fs/types").VirtualFs;
    stale: number;
};
